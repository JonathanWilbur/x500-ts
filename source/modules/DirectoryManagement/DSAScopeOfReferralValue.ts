/**
 * `DSAScopeOfReferralValue ::= INTEGER {dmd(0), country(1), global(2)}`
 */
enum DSAScopeOfReferralValue {
    dmd = 0,
    country = 1,
    global = 2,
}

export default DSAScopeOfReferralValue;
