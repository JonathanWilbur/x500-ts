import { ObjectIdentifier } from "asn1-ts";

/**
 * `SupportedApplicationContexts ::= SET OF OBJECT IDENTIFIER`
 */
type SupportedApplicationContexts = ObjectIdentifier[];
export default SupportedApplicationContexts;
