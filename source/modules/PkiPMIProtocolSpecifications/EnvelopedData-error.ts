/**
 * `EnvelopedData-error ::= ENUMERATED {
 *   noReason                           (0),
 *   ... }`
 */
enum EnvelopedData_error {
    noReason = 0,
}

export default EnvelopedData_error;
