/**
 * `ImposedSubset ::= ENUMERATED {baseObject(0), oneLevel(1), wholeSubtree(2),...}`
 */
enum ImposedSubset {
    baseObject = 0,
    oneLevel = 1,
    wholeSubtree = 2,
}

export default ImposedSubset;
